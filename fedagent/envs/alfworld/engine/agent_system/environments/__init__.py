# Vendored ALFWorld engine anchor. Intentionally EMPTY: the original verl-agent
# agent_system/environments/__init__.py imported env_manager (-> the old verl 0.3.x),
# which this overlay does not use. The engine only needs partition_strategy +
# env_package.alfworld, both siblings here, so no package-init imports are required.
