# TASK-HET hardness | GRPO | webshop | Qwen2.5-1.5B-Instruct
# auto-generated by tools/verl08_migration/gen_paper_configs.py -- mirrors the original
# config/ tree (structure + naming); contents are verl-0.8 run_fed.py configs.

env_kind: webshop
env_spec: config/envs/webshop_15.yaml
val_env_spec: config/envs/webshop_15_val.yaml
output_dir: /tmp/xbb9020_fedpaper/task_heterogeneity/webshop_grpo_hardness
model_path: Qwen/Qwen2.5-1.5B-Instruct                  # HF id; offline clusters: --model-path <local snapshot>

# --- federation protocol ---
total_clients: 100
clients_per_round: 2
total_rounds: 70
epochs_per_round: 3
base_seed: 42

# --- 4-GPU FSDP recipe + unperturbed validation ---
n_gpus_per_node: 4
total_training_steps: 0                             # 0 => full E epochs/round (no per-round step cap)
save_freq: 100000                                   # save only the round's last step
test_freq: 5
val_before_train: true
val_temperature: 0.4
wait_between_clients: 8
min_goals_per_client: 100
webshop_pool_size: 16
webshop_base_port: 8433
search_return_n: 50                               # engine default (matches the original non-het baselines)

# --- heterogeneity ---
partition_strategy: hardness
success_std: 256
trajectories_file: data/hardness/qwen2.5-1.5b_webshop_trajectories.json   # trained-checkpoint reference labels (shipped; see data/hardness/README.md)

client_overrides:
  - data.train_batch_size=8
  - data.max_prompt_length=2048
  - data.max_response_length=6144
  - actor_rollout_ref.actor.ppo_mini_batch_size=8
  - actor_rollout_ref.rollout.n=8
  - actor_rollout_ref.rollout.prompt_length=2048
  - actor_rollout_ref.rollout.response_length=6144
  - actor_rollout_ref.rollout.max_model_len=8192
  - actor_rollout_ref.rollout.gpu_memory_utilization=0.6
