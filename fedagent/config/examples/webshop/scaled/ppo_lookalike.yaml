# PPO (adv_estimator=gae) x ENV-heterogeneity (Lookalike Injection) -- the algorithm-ablation
# arm: does the Input-Dynamics Asymmetry (env-het hurts FedAvg) still hold under PPO+critic,
# not just GRPO? Same lookalike partition + kwargs as examples/webshop/scaled/lookalike.yaml; only
# the algorithm changes (gae + a federated critic). Mirrors the original
# env_heterogeneity/lookalike_injection_ppo arm. Qwen2.5-1.5B @ 15 turns, 2 clients x 4 rounds,
# 4 GPUs. The federated critic is FedAvg'd + carried forward exactly like the actor (run_fed).

env_kind: webshop
env_spec: config/envs/webshop_15.yaml
output_dir: /tmp/xbb9020_fedagent_scaled_ppo_lookalike
model_path: Qwen/Qwen2.5-1.5B-Instruct                  # HF id; offline clusters: --model-path <local snapshot>

adv_estimator: gae

total_clients: 2
clients_per_round: 2
total_rounds: 4
epochs_per_round: 1
base_seed: 42

n_gpus_per_node: 4
total_training_steps: 4
save_freq: 4
wait_between_clients: 8

partition_strategy: lookalike          # env-variant V4: lookalike-injected catalog, UNIFORM goals
webshop_pool_size: 16
webshop_base_port: 8124                # distinct from homog-PPO(8120) and the GRPO arms

client_overrides:
  - data.train_batch_size=8
  - data.max_prompt_length=2048
  - data.max_response_length=4096
  - actor_rollout_ref.actor.optim.lr=1e-6
  - actor_rollout_ref.actor.ppo_mini_batch_size=8
  - actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=4
  - actor_rollout_ref.actor.use_kl_loss=true
  - actor_rollout_ref.actor.kl_loss_coef=0.01
  - actor_rollout_ref.actor.kl_loss_type=low_var_kl
  - actor_rollout_ref.actor.fsdp_config.param_offload=false
  - actor_rollout_ref.actor.fsdp_config.optimizer_offload=true
  - actor_rollout_ref.actor.checkpoint.save_contents=[model]
  - actor_rollout_ref.rollout.n=2
  - actor_rollout_ref.rollout.prompt_length=2048
  - actor_rollout_ref.rollout.response_length=4096
  - actor_rollout_ref.rollout.max_model_len=8192
  - actor_rollout_ref.rollout.gpu_memory_utilization=0.5
  - critic.optim.lr=1e-5
  - critic.model.use_remove_padding=true
  - critic.model.enable_gradient_checkpointing=true
  - critic.fsdp.param_offload=false             # verl 0.8: critic FSDP lives at critic.fsdp (not critic.model.fsdp_config)
  - critic.fsdp.optimizer_offload=true
  - critic.ppo_micro_batch_size_per_gpu=4
  - critic.checkpoint.save_contents=[model]
  - trainer.critic_warmup=0
