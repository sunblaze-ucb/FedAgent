# Tier-2d EQUIVALENCE ARM (hf_export=final) vs dev/t2_base.yaml: identical except the knob.
# 0.5B, seed 42, cleanup off) on the CROSS_ROUND path -- the arm the hf_export/lanes knobs
# diff against (they only exist on the persistent path). Compare round_2 aggregated actor.
model_path: ""
output_dir: /gpfs/projects/b1222/userdata/canyu/kangyu/fedagent/runs/t2_ab/hfexport
rollout_mode: concat
cross_round: true
total_clients: 2
clients_per_round: 2
total_rounds: 2
epochs_per_round: 1
base_seed: 42
n_gpus_per_node: 4
total_training_steps: 2
save_freq: 2
cleanup_checkpoints: false
hf_export: final                                    # Tier-2d ARM: skip per-round merger; worker reloads aggregated shards
