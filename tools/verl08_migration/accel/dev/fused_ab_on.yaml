# FUSED-KERNELS EQUIVALENCE A/B, ON arm (+fused triton): the established TinyGuess full-loop equivalence rig
# (2 client x 2 round x 2 step, GRPO, concat, 0.5B, seed 42, cleanup off). Compare final aggregated
# actor shards vs the ON arm: bar = max|Delta| <= 1e-4 (bf16 noise floor).
model_path: ""
output_dir: /gpfs/projects/b1222/userdata/canyu/kangyu/fedagent/runs/fused_ab/on
rollout_mode: concat
persistent: false
total_clients: 2
clients_per_round: 2
total_rounds: 2
epochs_per_round: 1
base_seed: 42
n_gpus_per_node: 4
total_training_steps: 2
save_freq: 2
cleanup_checkpoints: false
client_overrides:
  - actor_rollout_ref.model.use_fused_kernels=True
  - actor_rollout_ref.model.fused_kernel_options.impl_backend=triton
