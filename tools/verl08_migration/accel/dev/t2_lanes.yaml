# #3-LANES EQUIVALENCE ARM (parallel_clients=2) vs dev/t2_base.yaml: identical except the knob.
# 0.5B, seed 42, cleanup off) on the CROSS_ROUND path -- the arm the hf_export/lanes knobs
# diff against (they only exist on the persistent path). Compare round_2 aggregated actor.
model_path: ""
output_dir: /gpfs/projects/b1222/userdata/canyu/kangyu/fedagent/runs/t2_ab/lanes
rollout_mode: concat
cross_round: true
total_clients: 2
clients_per_round: 2
total_rounds: 2
epochs_per_round: 1
base_seed: 42
n_gpus_per_node: 4
total_training_steps: 2
save_freq: 2
cleanup_checkpoints: false
parallel_clients: 2                                 # #3 lanes ARM: 2 clients concurrent on 2+2 GPUs (FSDP ws 2 vs 4)
